{% set version = environ.get("VERSION", "0.0.0") %}

package:
  name: brightpath
  version: {{ version }}

source:
  path: ..

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install --no-deps --ignore-installed . -vv
  script_env:
    - VERSION
    - CONDA_BLD_PATH

requirements:
  host:
    - python >=3.12
    - pip
    - setuptools >=77
    - wheel
    - pyyaml >=6
    - xlsxwriter >=3
  run:
    - python >=3.12
    - bw2io
    - olca-schema >=2.6.2,<3
    - pyyaml >=6
    - xlsxwriter >=3

test:
  imports:
    - brightpath
    - yaml
    - olca_schema
  requires:
    - python >=3.12
    - pytest
    - bw2io
    - olca-schema >=2.6.2,<3
    - xlsxwriter >=3

about:
  home: https://github.com/romainsacchi/brightpath
  license: BSD-3-Clause
  license_family: BSD
  summary: 'Validate, migrate, and exchange life-cycle inventories across LCA formats.'
